# Railway Frontend Nixpacks Configuration
[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]  
cmds = ["npm ci --only=production"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run preview -- --host 0.0.0.0 --port $PORT"